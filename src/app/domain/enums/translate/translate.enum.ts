/**
 * @description
 * Enumeración para los idiomas soportados por la aplicación.
 * Se utiliza para centralizar las claves de internacionalización (i18n).
 * * @enum {string}
 */
export enum TranslateEnum {
  EN = "en",
  ES = "es",
}
